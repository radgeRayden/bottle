import .src.runtime
let window = (import .src.window)
let gpu = (import .src.gpu)
let main = (import .src.main)

vvv bind bottle
do
    let run = main.run
    let
        window
        gpu

    locals;

bottle
